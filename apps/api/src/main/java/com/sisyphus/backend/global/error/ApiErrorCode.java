package com.sisyphus.backend.global.error;

public enum ApiErrorCode {
    INVALID_ARGUMENT,
    VALIDATION_ERROR,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    INTERNAL_SERVER_ERROR
}