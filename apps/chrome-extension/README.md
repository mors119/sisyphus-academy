# Chrome Extension

WXT + React browser extension for quick capture flows in Sisyphus Academy.

## Commands

```bash
bun install
bun run dev
bun run build
bun run zip
bun run compile
```

## Structure

- `entrypoints/background.ts`: extension background logic
- `entrypoints/popup/`: popup UI and feature code
- `public/`: icons and static extension assets
